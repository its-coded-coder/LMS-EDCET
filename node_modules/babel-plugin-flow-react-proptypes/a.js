
var {readFileSync} = require('fs');

var text = readFileSync('src/__test__/fixtures/function-with-generic-type-annotation.input.js', 'utf-8');

var {parse} = require('babylon');
var ast = parse(text, {
  plugins: ['flow'],
  sourceType: 'module',
});
var fn = ast.program.body[1].declaration;
console.log(JSON.stringify(fn, null, 2));

var {hasReactElementTypeAnnotationReturn} = require('./lib/util.js');
console.log(hasReactElementTypeAnnotationReturn(fn));

